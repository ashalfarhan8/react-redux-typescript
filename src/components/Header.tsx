import React from "react";
import logo from "../logo.svg";

const Header = () => {
  return (
    <header className="App-header py-4">
      <img src={logo} className="App-logo" alt="logo" />
      <p>Welcome to React, Redux, and Typescript</p>
    </header>
  );
};

export default Header;
